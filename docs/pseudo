cards = [...]

fun initialize(numberOfPlayers) {

stack = new Stack(cards)
stack.shuffle()
playersCards = stack.fetchPlayersCards(numberOfPlayers)
players = []

for playerCard in playersCards:
	player = new Player(...)
	players.add(player)

this.players = players
this.stack = stack;
this.playersQueue = shuffle(this.players)
}

fun getCurrentPlayer()
return this.playersQueue[0]

fun offerMove()
this.card = stack.popCard()

fun declineMove()
this.playersQueue.push(this.playersQueue.pop())

fun acceptMove()
playersQueue[0]

game = new Game()
game.initialize()

while !game.isEnd():
move = game.offerMove()

move.getActor()

userInput = input()

if userInput == 'accept':
	move.accept()

if userInput == 'decline':
	move.decline()

if userInput == 'trade':
	toWho = input()
	price = input()
	move.trade(100, player2)

game.next()


